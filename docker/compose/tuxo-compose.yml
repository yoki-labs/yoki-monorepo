version: "3.7"
services:
    bot:
        image: ghcr.io/yoki-labs/tuxedo/bot:latest
        container_name: TUXEDO_BOT
        restart: unless-stopped
        networks:
            - main-network

networks:
    main-network:
        driver: bridge
